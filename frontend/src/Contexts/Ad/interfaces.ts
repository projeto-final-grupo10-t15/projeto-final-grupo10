export interface iChildren {
    children: React.ReactNode;
}

export interface iAds {
    id: number;
    // name: string;
    // estado: string;
    // areaAtuacao: string;
    // imagem: string;
    // descricao: string;
    // userId: number;
  }