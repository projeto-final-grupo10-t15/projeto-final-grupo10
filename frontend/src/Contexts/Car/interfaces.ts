export interface iChildren {
    children: React.ReactNode;
}

export interface iCars {
    id: number;
    // name: string;
    // estado: string;
    // areaAtuacao: string;
    // imagem: string;
    // descricao: string;
    // userId: number;
  }